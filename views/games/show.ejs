<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('../partials/head.ejs') %>
    <title>Show</title>
</head>
<body>
    <%- include ('../partials/nav.ejs') %>
    <h1><%= game.name %></h1>
    <img src="<%= game.image %>" alt="<%= game.name %>">
    <%- include ('../partials/userButtons.ejs', {currentGame: game}) %>
    <h4><%= game.year %></h4>
    <h3>Average Rating: <%= game.rating %></h3>
    <h3>Complexity Rating: <%= game.complexity %></h3>
    <h3>Player Count: <%= game.playerCount %></h3>
    <p id="description"><%= game.description %></p>
    <p id="creater"><small>Made By <%= game.designer %> & <%= game.publisher %></small></p>
    <% if (currentUser) { %>
    <a href="/games/<%= game.id %>/edit"><button>Edit</button></a>
    <form action="/games/<%= game.id %>?_method=DELETE" method="POST">
        <input type="submit" name="delete" id="delete" value="DELETE">
    </form>
    <% } %>
    
</body>
</html>